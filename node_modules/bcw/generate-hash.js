#!/usr/bin/env node
require("./check-update");
let bcrypt = require("bcryptjs");

async function getHash() {
  console.log("Preparing to hash:", process.argv[2]);
  let result = await bcrypt.hash(process.argv[2], 10);
  console.log("Hashed result", result);
}

getHash();
